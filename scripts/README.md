# UrbanPulse Operational Scripts (`scripts/`)

This directory contains development automation, setup, seeding, and deployment scripts.

## Key Scripts

- `setup_dev.ps1`: Automated developer bootstrapping (virtual environments, npm dependency stubs, env file generation).
- `seed_db.py`: Spatial database seeding script for Mysuru and Bengaluru boundaries.
